# esbg 0.0.0.9000

Initial development version.

- Added `esbg()` for Equal-Size Binary Grouping.
- Added `generate_blobs()` for simulated two-dimensional examples.
- Added `same_partition()` for comparing binary partitions up to label switching.
- Added print, summary, and plot methods for ESBG fits.
