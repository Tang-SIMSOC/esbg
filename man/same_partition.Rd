% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esbg.R
\name{same_partition}
\alias{same_partition}
\title{Compare two binary partitions}
\usage{
same_partition(x, y, na_rm = TRUE)
}
\arguments{
\item{x}{A vector of group assignments, or an object returned by \code{\link[=esbg]{esbg()}}.}

\item{y}{A vector of group assignments, or an object returned by \code{\link[=esbg]{esbg()}}.}

\item{na_rm}{Logical. If \code{TRUE}, compare only observations assigned in both
partitions. If \code{FALSE}, missingness must also match.}
}
\value{
\code{TRUE} if the partitions are identical up to label switching;
otherwise \code{FALSE}.
}
\description{
Checks whether two binary group assignments represent the same partition,
allowing the group labels to be swapped. Missing values are ignored by default,
which is useful when \code{odd_method = "average"} or \code{odd_method = "remove"} leaves
one median observation unassigned.
}
\examples{
same_partition(c(1, 1, 2, 2), c(2, 2, 1, 1))
}
