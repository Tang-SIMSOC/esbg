% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esbg.R
\name{generate_blobs}
\alias{generate_blobs}
\title{Generate synthetic blob data}
\usage{
generate_blobs(
  n = 100,
  n_blobs = 2,
  sizes_blobs = NULL,
  spreads_blobs = NULL,
  seed = NULL,
  include_id = FALSE,
  rescale = TRUE
)
}
\arguments{
\item{n}{Number of observations.}

\item{n_blobs}{Number of blobs.}

\item{sizes_blobs}{Optional blob sizes. If \code{NULL}, blob sizes are as equal as
possible. If \code{sizes_blobs} is a positive integer vector that sums to \code{n}, it
is used as exact blob counts. Otherwise, \code{sizes_blobs} is interpreted as
relative weights. For example, with \code{n = 300}, \code{sizes_blobs = c(1, 2, 3)}
produces blob sizes
\code{c(50, 100, 150)}.}

\item{spreads_blobs}{Optional blob spreads, used as the standard deviation
for each blob. If \code{NULL}, spreads increase evenly from \code{0.4} to \code{0.8}.}

\item{seed}{Optional random seed.}

\item{include_id}{Logical. If \code{TRUE}, include an \code{id} column.}

\item{rescale}{Logical. If \code{TRUE}, generate unbounded normal blobs and
min-max rescale \code{x} and \code{y} to \verb{[0, 1]}. If \code{FALSE}, generate bounded
normal blobs directly inside \verb{[0, 1]}.}
}
\value{
A data frame with columns \code{x} and \code{y}, plus \code{id} when
\code{include_id = TRUE}.
}
\description{
Creates a two-dimensional synthetic dataset with one or more natural blobs.
This is useful for demonstrating ESBG on data with visible spatial structure.
}
