% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esbg.R
\name{esbg}
\alias{esbg}
\title{Equal-Size Binary Grouping for polarization measurement}
\usage{
esbg(
  data,
  vars = NULL,
  n_starts = 50,
  max_iter = 100,
  tol = 1e-08,
  delta = 1,
  scale = FALSE,
  na_rm = FALSE,
  exclude_vars = c("id"),
  cluster_method = c("em", "centroid"),
  odd_method = "average"
)
}
\arguments{
\item{data}{A data frame containing individual-level observations.}

\item{vars}{Optional character vector of numeric columns defining the opinion
space. If \code{NULL}, numeric columns are inferred from \code{data}, excluding common
identifier columns such as \code{id}.}

\item{n_starts}{Number of random restarts. For \code{cluster_method = "em"}, each
restart begins from a random equal-size partition. For
\code{cluster_method = "centroid"}, the input order is randomly shuffled before
each call to \code{anticlust::balanced_clustering()}. Across starts, ESBG keeps
the partition with the smallest within-group sum of squares, equivalent to
the smallest \code{W}. The starts are not averaged.}

\item{max_iter}{Maximum number of EM iterations per random start. Used only
when \code{cluster_method = "em"}; ignored when \code{cluster_method = "centroid"}.}

\item{tol}{Convergence tolerance for the within-group sum of squares. Used
only when \code{cluster_method = "em"}; ignored when
\code{cluster_method = "centroid"}.}

\item{delta}{Positive normalizing parameter used to compute the polarization
index \code{P = (1 / delta) * B / (W + 1)}. The default is \code{1}. For comparing
multiple datasets, provide the same \code{delta} for each call.}

\item{scale}{Logical. If \code{TRUE}, selected variables are standardized before
ESBG is estimated.}

\item{na_rm}{Logical. If \code{TRUE}, rows with missing values in \code{vars} are
omitted from estimation. If \code{FALSE}, missing values trigger an error.}

\item{exclude_vars}{Character vector of columns to exclude when \code{vars = NULL}.
Ignored when \code{vars} is supplied.}

\item{cluster_method}{Clustering backend. \code{"em"} uses the constrained
k-means/EM implementation described in Tang et al. (2022). \code{"centroid"}
uses \code{anticlust::balanced_clustering(..., method = "centroid")}.}

\item{odd_method}{How to handle an odd number of complete observations.
\code{"error"} stops with an error. \code{"remove"} removes the observation closest
to the component-wise median and reports ESBG for the remaining even sample.
\code{"average"} removes that observation, estimates ESBG on the remaining even
sample, evaluates both ways of assigning the median observation to one of
the two groups, and averages the resulting scores. \code{"min"} reports the
assignment of the median observation that gives the smaller within-group sum
of squares. The default is \code{"average"}.}
}
\value{
An object of class \code{"esbg"} containing group assignments, centroids,
scores, and estimation metadata.
}
\description{
Estimate an Equal-Size Binary Grouping (ESBG) partition for numeric opinion
data and compute within-group dispersion and between-group separation.
}
\examples{
data <- generate_blobs(n = 100)
fit <- esbg(data, vars = c("x", "y"), n_starts = 20)
fit_all_numeric <- esbg(data, n_starts = 20)
summary(fit)
plot(fit)
}
